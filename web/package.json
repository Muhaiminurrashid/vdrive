{
  "private": true,
  "scripts": {
    "css": "npx @tailwindcss/cli -i input.css -o public/styles.css",
    "css:watch": "npx @tailwindcss/cli -i input.css -o public/styles.css --watch"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.3.2",
    "tailwindcss": "^4.3.2"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}
